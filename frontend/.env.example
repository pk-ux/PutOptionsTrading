# Frontend environment variables
# Copy to .env and fill in values

# API URL - defaults to same origin in production, localhost:8000 in dev
VITE_API_URL=http://localhost:8000

# Clerk (Phase 2)
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
